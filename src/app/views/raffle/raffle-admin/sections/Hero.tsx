import HeroWrapper from '@/app/components/HeroWrapper';
import React from 'react';

const Hero = () => {
  return (
    <section>
      <HeroWrapper></HeroWrapper>
    </section>
  );
};

export default Hero;
